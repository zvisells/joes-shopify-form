{
  "name": "shopify-form-webhook",
  "version": "1.0.0",
  "description": "Webhook endpoint for Shopify form submissions with Resend integration",
  "main": "api/contact.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 